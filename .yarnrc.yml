# Shared Yarn download cache (host + Docker bind mount at .cache/yarn).
# Yarn checks this cache before hitting the network.
cacheFolder: ./.cache/yarn
nodeLinker: node-modules
enableGlobalCache: false
# Prefer cache hits; only fetch when a package is missing from .cache/yarn.
compressionLevel: mixed
